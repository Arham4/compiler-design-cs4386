class testClass
{
	int testInt;
	bool testBool;
	char testChar;
	float testFloat;

	void testNestedIfs()
	{
		if (true)
			if (~true)
				if (false)
					read(x);
				else
					if (yes == false)
						print(2.0);
					else
						x = 23;
			else
				y = 37;

		return ;

	}

	void testNestedIfsInWhiles ()
	{
			int newTestVal;
    		bool thisWorks = true;
    		final char theMan = '\'';
		    float testDec = 3.14;

		    newTestVal = 4;
            testInt = 5;
            if (thisWorks)
            	while(true)
            	    while(false)
            	        if(newTestVal > 3)
            	            if (y > 3)
            	                if (x < 2)
            		                read(x);
            		            else
                                    read(thisName);
                            else if (y < 3)
                                read(test);

	}
}
